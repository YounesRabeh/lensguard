[package]
name = "camera-pipewire"
description = "PipeWire adapter boundary for LensGuard"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
camera-core = { version = "0.5.0", path = "../camera-core" }
pipewire = "0.10.0"
thiserror = "2.0.19"
tracing = "0.1.44"
